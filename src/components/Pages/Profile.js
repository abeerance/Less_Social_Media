import React from "react";
import classes from './Profile.module.css'

const Profile = () => {
    return(
        <div>
            <h2>Dis the prfile</h2>
        </div>
    )
};

export default Profile;
