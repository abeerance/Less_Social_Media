import React from "react";
import classes from "./Photos.module.css";

const Image = (image) => {
    console.log(image)
}

const Photos = (images) => {
    //        images.map((i) => <Image image={i} />)
    console.log(images)
    return (
        <h2>hi</h2>
    );
};

export default Photos;
